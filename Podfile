# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DMD' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DMD
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Database'
  pod 'Firebase/Analytics'

  target 'DMDTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DMDUITests' do
    # Pods for testing
  end

end

target 'FoodAppClip' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FoodAppClip

end
